name: Build Native APK (MissNet Native)

on:
  push:
    branches:
      - main
    paths:
      - 'miss_net_native/**'
      - '.github/workflows/build_native_apk.yml'
  pull_request:
    paths:
      - 'miss_net_native/**'
      - '.github/workflows/build_native_apk.yml'
  workflow_dispatch:

jobs:
  build-debug-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Generate new debug signing key
        working-directory: miss_net_native
        run: |
          mkdir -p app
          keytool -genkeypair \
            -keystore debug.keystore \
            -alias missnet-debug \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -storepass MissNet2026 \
            -keypass MissNet2026 \
            -dname "CN=miss_net, OU=dev, O=miss_net, L=SH, ST=SH, C=CN" -noprompt
          cat > app/signing.properties << 'EOF'
          keystore=app/debug.keystore
          keystore_password=MissNet2026
          key_alias=missnet-debug
          key_password=MissNet2026
          EOF

      - name: Grant execute permission for gradlew
        working-directory: miss_net_native
        run: chmod +x gradlew

      - name: Assemble Debug APK
        working-directory: miss_net_native
        run: |
          ./gradlew :app:assembleDebug --stacktrace 2>&1 | tee /tmp/gradle.log
          mkdir -p /tmp/dist
          find app/build/outputs/apk/ -name "*.apk" -exec cp {} /tmp/dist/MissNet-debug.apk \;

      - name: Upload Debug APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: MissNet-Debug-APK-${{ github.run_number }}
          path: /tmp/dist/MissNet-debug.apk
          if-no-files-found: warn
          retention-days: 14

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: gradle-build-log-${{ github.run_number }}
          path: /tmp/gradle.log
          if-no-files-found: warn
          retention-days: 7
